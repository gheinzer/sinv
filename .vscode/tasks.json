{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start development processes",
            "type": "shell",
            "command": "cd frontend && (ng serve &) && (ng build --watch &) && cd ../backend && (tsc --watch &) && cd .. && supervisor dist/backend/index.js",
            "problemMatcher": []
        }
    ]
}
